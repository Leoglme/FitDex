/**
 * Props du composant SuccessBurst.
 */
export interface SuccessBurstProps {
  /** Affiche et déclenche l'animation quand `true`. */
  show: boolean
  /** Titre principal affiché (ex: "Séance terminée !"). */
  title?: string
  /** Sous-titre / message de motivation. */
  subtitle?: string
}
